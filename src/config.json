{
  "paths": {
    "data_dir": "root directory pf the dataset",
    "checkpoint_dir": "checkpoints",
    "log_file_path": "log.txt"
  },

  "training": {
    "val_proportion": 0.10,
    "lr": 0.001,
    "batch_size": 128,
    "num_epochs": 100,
    "epoch_start": 0,
    "last_checkpoint_path": "None"
  }
}
